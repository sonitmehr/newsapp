import React  from 'react'

const LoadingBar = ()=> {
    return (
      <LoadingBar
        color="#f11946"
        progress={this.props.progress}
        height = {3}
      />
    );
}

export default LoadingBar